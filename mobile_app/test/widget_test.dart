import 'package:field_app/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('DisasterNetApp can be instantiated', () {
    const app = DisasterNetApp();
    expect(app, isNotNull);
  });
}
